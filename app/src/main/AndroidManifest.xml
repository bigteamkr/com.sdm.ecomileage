<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sdm.ecomileage">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>

    <application
        android:name="com.sdm.ecomileage.SdmEcoMileageApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sdm_eco_mileage"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.sdm.ecomileage.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Sdm_eco_mileage"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />

                <!-- Redirect URI: "kakao${NATIVE_APP_KEY}://oauth" -->
<!--                <data android:host="oauth"-->
<!--                    android:scheme="kakaofb7903a5b79c6fb3d7172024332e682d" />-->
            </intent-filter>

        </activity>
    </application>

</manifest>